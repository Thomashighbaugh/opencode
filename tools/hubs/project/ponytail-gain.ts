import { HubSubcommandSpec } from "../../hub-data"

const spec: HubSubcommandSpec = {
  label: "ponytail-gain",
  description: "Show measured impact scoreboard",
  reminder: "Showing impact scoreboard.",
  inline: true,
  detailedDescription: "Displays impact metrics (LOC, cost, latency reduction) from Ponytail benchmark runs.",
}

export default spec
